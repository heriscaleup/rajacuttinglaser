---
// CTA.astro - Komponen Call-to-Action yang dapat digunakan kembali
import { SITE_CONFIG, WHATSAPP_MESSAGES } from '../utils/constants.js';
import WhatsAppButton from './WhatsAppButton.astro';

export interface Props {
  title?: string;
  description?: string;
  primaryText?: string;
  primaryLink?: string;
  secondaryText?: string;
  secondaryLink?: string;
  backgroundImage?: string;
  variant?: 'default' | 'gradient' | 'image' | 'minimal';
  size?: 'sm' | 'md' | 'lg';
  whatsappMessage?: string;
}

const {
  title = "Siap Memulai Proyek Laser Cutting Anda?",
  description = "Dapatkan konsultasi gratis dan quote terbaik untuk kebutuhan laser cutting Anda. Tim ahli kami siap membantu mewujudkan ide kreatif Anda.",
  primaryText = "Konsultasi Gratis",
  primaryLink = `https://api.whatsapp.com/send?phone=${SITE_CONFIG.whatsapp}&text=${encodeURIComponent(WHATSAPP_MESSAGES.default)}`,
  secondaryText = "Dapatkan Quote",
  secondaryLink = "/contact",
  backgroundImage,
  variant = 'default',
  size = 'md',
  whatsappMessage = WHATSAPP_MESSAGES.default
} = Astro.props;

const getVariantClasses = () => {
  switch (variant) {
    case 'gradient': return 'bg-gradient-to-r from-accent to-accent-blue text-background';
    case 'image': return 'relative text-white';
    case 'minimal': return 'bg-background text-text-main border border-accent';
    default: return 'bg-accent text-background';
  }
};

const getSizeClasses = () => {
  switch (size) {
    case 'sm': return 'py-12';
    case 'lg': return 'py-24 md:py-32';
    default: return 'py-16 md:py-20';
  }
};

const variantClasses = getVariantClasses();
const sizeClasses = getSizeClasses();
---

<section class={`${variantClasses} ${sizeClasses} relative overflow-hidden`}>
  {variant === 'image' && backgroundImage && (
    <div class="absolute inset-0 z-0">
      <img src={backgroundImage} alt="Background" class="w-full h-full object-cover" loading="lazy"/>
      <div class="absolute inset-0 bg-green-900/80"></div>
    </div>
  )}

  {variant !== 'minimal' && (
    <div class="absolute inset-0 z-10">
      <svg class="w-full h-full opacity-10" viewBox="0 0 100 100" preserveAspectRatio="none">
        <defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse">
          <path d="M10 0L0 0 0 10" fill="none" stroke="currentColor" stroke-width="0.5"/>
        </pattern></defs>
        <rect width="100" height="100" fill="url(#grid)"/>
      </svg>
    </div>
  )}

  <div class="relative z-20 container-custom text-center">
    <h2 class="heading-lg mb-6 fade-in-up text-text-main">{title}</h2>
    <p class={`mb-8 max-w-2xl mx-auto leading-relaxed fade-in-up ${variant==='minimal'?'text-text-secondary':'text-white/95'}`} style="animation-delay:0.2s;">
      {description}
    </p>
    <div class="flex flex-col sm:flex-row items-center justify-center space-y-4 sm:space-y-0 sm:space-x-6 fade-in-up" style="animation-delay:0.4s;">
      <WhatsAppButton
        message={whatsappMessage}
        className={`text-lg px-8 py-4 font-bold rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg w-full sm:w-auto ${variant==='minimal'?'bg-accent hover:bg-accent-blue text-background shadow-glow':'bg-accent hover:bg-accent-blue text-background shadow-glow'}`}
        source="cta_section"
      />
      <a href={secondaryLink}
         class="inline-flex items-center px-8 py-4 text-lg font-semibold rounded-lg border-2 transition-all duration-300 w-full sm:w-auto
           ${variant==='minimal'?'border-accent text-accent hover:bg-accent hover:text-background':'border-accent text-accent hover:bg-accent hover:text-background shadow-lg'}">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m-6-8h6"/>
        </svg>
        {secondaryText}
      </a>
    </div>

    <div class="mt-12 fade-in-up" style="animation-delay:0.6s;">
      <div class="flex flex-col sm:flex-row items-center justify-center space-y-4 sm:space-y-0 sm:space-x-8 text-sm font-medium ${variant==='minimal'?'text-text-secondary':'text-white/95'}">
        <div class="flex items-center space-x-2">
          <svg class="w-5 h-5 ${variant==='minimal'?'text-accent':'text-accent'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <span>Respon dalam 24 jam</span>
        </div>
        <div class="flex items-center space-x-2">
          <svg class="w-5 h-5 ${variant==='minimal'?'text-accent':'text-accent'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <span>Konsultasi gratis</span>
        </div>
        <div class="flex items-center space-x-2">
          <svg class="w-5 h-5 ${variant==='minimal'?'text-accent':'text-accent'}" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
          <span>Tanpa komitmen</span>
        </div>
      </div>
    </div>
  </div>

  {variant!=='minimal' && (
    <div class="absolute inset-0 z-10 pointer-events-none">
      <div class="absolute top-10 right-10 w-20 h-20 border border-accent/20 rounded-full"></div>
      <div class="absolute bottom-10 left-10 w-16 h-16 border border-accent/10 rounded-full"></div>
      <div class="absolute top-1/2 left-20 w-2 h-2 bg-accent/30 rounded-full animate-pulse"></div>
      <div class="absolute top-1/3 right-20 w-3 h-3 bg-accent/20 rounded-full animate-bounce"></div>

      <div class="absolute top-20 left-1/4 w-1 h-1 bg-accent/40 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-20 right-1/4 w-1.5 h-1.5 bg-accent/30 rounded-full animate-bounce" style="animation-delay: 2s;"></div>
    </div>
  )}
</section>

<style>
  @keyframes fadeInUp { 
    from { 
      opacity: 0; 
      transform: translateY(30px);
    } 
    to {
      opacity: 1; 
      transform: translateY(0);
    } 
  }
  
  .fade-in-up { 
    animation: fadeInUp 0.8s ease-out forwards; 
    opacity: 0; 
    transform: translateY(30px); 
  }
  
  /* Enhanced button hover effects */
  .btn-hover-lift {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .btn-hover-lift:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
  }
</style>

